{
  "name": "musicfree-pikpak-plugin",
  "private": true,
  "version": "0.0.5",
  "description": "MusicFree PikPak WebDAV 插件（TypeScript 源码，tsc 编译为 Hermes 可用的 ES5）",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
